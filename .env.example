# Runtime Integrity Check

# Set to 'true' to enable SVG hash verification on app boot
# This adds ~50-100ms startup time but catches deployment artifact mismatches
# Production builds should enable this in CI/CD pipeline
VITE_VERIFY_RUNTIME_INTEGRITY=false

# Example: Enable for production deploy verification
# VITE_VERIFY_RUNTIME_INTEGRITY=true

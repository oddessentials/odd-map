#!/usr/bin/env sh
# Pre-commit: auto-fix and stage corrections on staged files only
# Uses lint-staged for efficient, targeted fixes
# Full verification (typecheck + tests) runs in pre-push

npx lint-staged
